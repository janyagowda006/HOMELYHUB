# Frontend Environment Variables
# If deploying Frontend separately (e.g. on Vercel or Netlify),
# set VITE_BACKEND_URL to your deployed backend URL.
# If deploying unified (backend serves frontend) or locally, leave it empty.

VITE_BACKEND_URL=https://your-backend-api.onrender.com
